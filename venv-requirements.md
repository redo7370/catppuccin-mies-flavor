# Python Virtual Environment

To use the Python theme preview tool (`test-theme.py`), set up a virtual environment and install the requirements:

```sh
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## requirements
```
PyQt6
```

---
